{
  "name": "opspilot",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "opspilot.es · web de marketing (Astro, HTML estático). Identidad «horizonte artificial».",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/postbuild.mjs",
    "preview": "astro preview",
    "check": "node scripts/revisar.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^5.18.2"
  }
}
